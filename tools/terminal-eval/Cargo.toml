[package]
name = "vibra-terminal-eval"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
alacritty_terminal = "=0.26.0"

[build-dependencies]
cc = "1.2"

[profile.release]
lto = "thin"
codegen-units = 1
